
(function(obj) {

var sqrt = Math.sqrt,
	pow = Math.pow,
	atan2 = Math.atan2,
	abs = Math.abs,
	min = Math.min,
	max = Math.max;
      

obj.base = Class.extend({
	init: function(){
	
  	},
  	render: function(ctx) {
  	
  	},
  	containsPoint: function(x, y) {
  	
  	},
  	drawHandles: function() {
  	
  	}
});

})(app.obj);

define(["app/init"], function(app) {
log(app);
/*
	var app = window.app;
	var loaded = CLOSED;
	
	app.sidebar = {
		drawFileManager: drawFileManager
	};
	
	function drawFileManager() {
		
		if (app.filemanager.is(":visible")) {
			app.filemanager.hide();
			app.container.show();
		}
		else {
			app.filemanager.height(app.container.height()).width(app.container.width());
			$.tmpl("fileManager", loaded.files).appendTo(app.filemanager.find("ul").empty());
			app.filemanager.show();
			app.container.hide();
		}
	}*/
});



(function() {
})();